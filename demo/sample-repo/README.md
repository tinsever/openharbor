# Demo sample repository

This directory is a tiny mounted repo for `demo/run.ts`. The Harbor host treats it as read-only at the capability layer; edits land in the session overlay until publish is approved (not applied in this demo).
